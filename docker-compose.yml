version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - .:/spr_scraper
      - node_modules:/spr_scraper/node_modules
    command: npm run dev

volumes:
  node_modules:
